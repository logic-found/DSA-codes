#include<iostream>
#include<stack>
stack<int> st;
int cap;
void search(int val,int n){
	if(st.top()==val){
		x=val;
	}
	int n=st.top();
	st.pop();
	search(val,);
	st.push(n);
	if(st.size()==n-1){
		st.push())
	}
	
}
void insertBottom(){
	if(st.size()==1){
		st.pop();return;}
		
	int num=st.top();
	st.pop();
	insertBottom(st,element);
	st.push(num);}

void push(int val){
	if(cap-st.size()<1) bottom();
	st.push(val);
}

void get(int val){
	
}
int main(){
	
	
}